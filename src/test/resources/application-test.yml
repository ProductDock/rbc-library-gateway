spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://placeholder
          jwk-set-uri: http://placeholder
cors:
  allowed:
    origins: http://localhost:3000